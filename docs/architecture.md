# Architecture

M3 Glass is organized as a design-system monorepo. Applications demonstrate, document and validate the reusable packages; they are not the architectural center of the project.

The public documentation sitemap follows the broad model:

- Foundations
- Glass
- Components
- Patterns
- Develop

Deep documentation URLs should use stable path-based routes such as `/components/button` rather than query-string navigation.
