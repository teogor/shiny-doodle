const content: Record<string,{title:string;description:string}>={color:{title:'Color',description:'Semantic color roles and adaptable schemes.'},typography:{title:'Typography',description:'Expressive type roles for readable interfaces.'},surfaces:{title:'Glass surfaces',description:'Layered translucent surfaces with controlled optical depth.'},blur:{title:'Blur',description:'Use blur as a depth signal, not decoration.'},button:{title:'Button',description:'Actions with Material-inspired hierarchy and M3 Glass styling.'},card:{title:'Card',description:'Contain related information inside a responsive glass surface.'},'getting-started':{title:'Getting started',description:'Install the workspace packages and start composing your first interface.'}};
export default async function Detail({params}:{params:Promise<{section:string;slug:string}>}){const {section,slug}=await params;const item=content[slug]??{title:slug.split('-').map(x=>x[0]?.toUpperCase()+x.slice(1)).join(' '),description:`Documentation for ${slug} in the ${section} section.`};return <main className="docs-page detail"><a href={`/${section}`}>← Back to {section}</a><p className="eyebrow">{section}</p><h1>{item.title}</h1><p>{item.description}</p><section className="m3-glass docs-preview"><h2>Live preview</h2><p>This route is ready to be replaced with MDX content and component examples.</p></section></main>}
