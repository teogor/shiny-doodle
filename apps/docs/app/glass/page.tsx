const items=['Principles','Surfaces','Blur','Transparency','Refraction','Borders','Highlights','Shadows','Layering','Performance'];
export default function Glass(){return <main className="docs-page"><p className="eyebrow">GLASS</p><h1>The optical language.</h1><p>Glass is a first-class system: surfaces, translucency, blur and depth are expressed through reusable tokens and primitives.</p><div className="section-grid">{items.map((item,i)=><a className="doc-link" href={`/glass/${item.toLowerCase()}`} key={item}><span>0{i+1}</span><h2>{item}</h2><strong>Explore →</strong></a>)}</div></main>}
