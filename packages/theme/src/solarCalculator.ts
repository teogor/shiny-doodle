export interface SolarSchedule{sunriseHour:number;sunsetHour:number;timezone:string;}
function estimateLatitudeBand(timezone:string):'polar'|'temperate'|'tropical'{const tz=timezone.toLowerCase();if(tz.includes('reykjavik')||tz.includes('tromso')||tz.includes('arctic'))return'polar';if(tz.includes('africa/lagos')||tz.includes('asia/singapore')||tz.includes('america/bogota')||tz.includes('asia/jakarta')||tz.includes('america/manaus'))return'tropical';return'temperate';}
function seasonalOffsetHours(date:Date){const dayOfYear=Math.floor((date.getTime()-new Date(date.getFullYear(),0,0).getTime())/86400000);return Math.sin(((dayOfYear-80)/365)*2*Math.PI)*1.6;}
export function getSystemSolarSchedule(referenceDate:Date=new Date()):SolarSchedule{const timezone=Intl.DateTimeFormat().resolvedOptions().timeZone;const band=estimateLatitudeBand(timezone);const seasonal=band==='polar'?seasonalOffsetHours(referenceDate)*2.2:seasonalOffsetHours(referenceDate);const baseSunrise=band==='tropical'?6.25:6.75;const baseSunset=band==='tropical'?18.25:18.75;return{sunriseHour:Math.max(4,baseSunrise-seasonal*.5),sunsetHour:Math.min(23,baseSunset+seasonal*.5),timezone};}
export function isNighttime(referenceDate:Date=new Date(),schedule?:SolarSchedule){const sched=schedule??getSystemSolarSchedule(referenceDate);const localHour=referenceDate.getHours()+referenceDate.getMinutes()/60;return localHour<sched.sunriseHour||localHour>=sched.sunsetHour;}
